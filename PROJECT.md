# 类 HOI4 抖音小游戏 · 开发文档

> 版本：v0.1 · 立项设计稿
> 技术栈：Cocos Creator 3.8 + TypeScript + Node.js 信令服务器
> 平台：抖音小游戏（主包 <4MB，总包 ≤20MB）

---

## 1. 项目定位

| 维度 | 决策 |
|---|---|
| 核心体验 | HOI4 式大战略经营，**双模式分层**：快速对局（10-15 分钟单局）+ 经典存档（长期不删档，玩家主动弃档才结束） |
| 玩法重心 | **工业建设、资源经营为核心卖点**；战争作为可选冲突玩法（非强制），新手引导优先教学工业与资源 |
| 单局时长 | 快速对局 10-15 分钟一局；经典存档单次会话 5-15 分钟可推进一个焦点，整局可跨数周持续游玩 |
| 联机 | 好友联机，借鉴 HOI4：**主机存档制 + drop in/out + AI 接管** |
| 商业化 | 无 IAP；单机激励视频+插屏；**联机模式无任何广告** |
| 国名 | **全架空代号**（铁十字联邦/赤色同盟等），零审核风险 |
| 变现闭环 | 联机拉 DAU → 单机模式变现 |

---

## 2. 核心决策记录

| 决策项 | 选择 | 理由 |
|---|---|---|
| 联机方案 | 轻量信令服务器 + 帧同步 | 抖音沙箱无 UDP/WebRTC，纯 P2P 不可行；服务器只转发输入帧，逻辑在客户端确定性跑 |
| 联机权威 | **Host 权威帧提交**（借鉴 HOI4 主机制） | Host 作为帧提交源 + 存档持有者，服务器仅中转 |
| 国名 | 全架空代号 | 抖音审核明确禁止战争题材使用真实国名/国旗/真实装备，直接架空规避 |
| 存档 | 本地持久存档，借鉴 HOI4 持续存档 | 玩家随时回来续玩，跨数周经营 |
| 时间控制 | 暂停 / 1x / 2x / 5x，保留 HOI4 手感 | 联机时 host 控制全局节奏 |
| 单局模型 | 双模式：快速对局（10-15 分钟）+ 经典存档（长期） | 快速对局适配碎片化场景降门槛，经典存档保留 HOI4 代入感 |

---

## 3. 玩法设计

### 3.1 核心循环

```
打开游戏 → 加载存档（1-2s）→ 进入世界地图
  ├─ 实时推演：资源产出 / 工业产出 / 补给流动 / 前线交火（自动）
  ├─ 玩家决策（暂停或运行时均可操作）：
  │   ├─ 资源采集与储备管理 ⭐ 核心系统
  │   ├─ 建筑模式：进入建造视图铺设工厂/基建 ⭐ 核心系统
  │   ├─ 工厂生产任务分配 + 空闲提醒 ⭐ 核心系统
  │   ├─ 国家焦点三选一（每 60s 刷新）
  │   ├─ 科研三选一（线性科技线）
  │   ├─ 调兵拉线（画前线 → 下达攻势）
  │   └─ 外交（结盟 / 发起区域争端 / 贸易）
  ├─ 时间控制：暂停 / 1x / 2x / 5x
  └─ 自动存档（每 30s + 关键事件）
```

### 3.2 资源系统（产出 + 储备 + 未使用保留）⭐

**设计原则**：借鉴 HOI4 的资源体系。资源由省份产出，自动进入国库储备；**未使用的资源保留到下一 tick**，不会清零。资源是建造、生产、贸易的硬约束。

#### 3.2.1 资源类型

| 资源 | 产出地 | 用途 |
|---|---|---|
| 钢铁 | 工业省份（矿区） | 建造工厂/基建、生产装甲/炮兵装备 |
| 石油 | 油田省份 | 装甲/战机/舰船运转、机械化部队补给 |
| 钨 | 特定矿区 | 高级装甲、穿甲弹生产 |
| 橡胶 | 热带省份/贸易 | 车辆、战机生产 |
| 铝 | 铝土矿区 | 飞机生产 |
| 政治点 | 首都每日自动 | 焦点、外交、法律、顾问 |

#### 3.2.2 产出与储备

```
每 tick：
  for 每个己方省份:
    if 省份有资源节点 and 省份有对应开采建筑:
      国库[资源] += 产出速率 × tick
  // 未使用资源保留，累加到下 tick（不清零）
```

- 产出速率受：省份基础设施等级、开采建筑等级、管控状态（管控区产出减半）影响
- 储备上限：受仓储建筑（储油罐/仓库）限制，超出上限的产出被丢弃并提示
- **关键规则**：未消耗的资源永久保留在储备池，跨 tick、跨会话、跨存档都不丢失

#### 3.2.3 消耗场景

| 消耗项 | 消耗资源 | 说明 |
|---|---|---|
| 建造工厂 | 钢铁 | 每级工厂消耗钢铁 × 等级 |
| 建造基建 | 钢铁 | 基础设施升级 |
| 生产装备 | 钢铁/钨/铝/橡胶 | 按装备类型消耗对应资源 |
| 部队运转 | 石油 | 装甲/机械化/空军每 tick 消耗 |
| 贸易购入 | 政治点/民厂 | 用民厂产能换他国资源 |

#### 3.2.4 资源不足处理

- 建造/生产任务**不取消**，而是**减速**（HOI4 机制）
- 缺资源 50%：建造/生产速度降为 50%
- 缺资源 100%：建造/生产暂停，UI 红色警告"缺钢铁"
- 部队缺石油：移动力 -80%、无法发起攻势

#### 3.2.5 数据结构

```typescript
type ResourceType = 'steel' | 'oil' | 'tungsten' | 'rubber' | 'aluminum' | 'political';

interface ResourceStockpile {
  steel: number;
  oil: number;
  tungsten: number;
  rubber: number;
  aluminum: number;
  political: number;
  // 储备上限（受仓储建筑影响）
  caps: { steel: number; oil: number; ... };
}

interface ResourceNode {
  provinceId: number;
  type: ResourceType;
  baseYield: number;       // 基础产出速率（每秒）
  buildingLevel: number;   // 开采建筑等级
  controlled: boolean;     // 是否被管控（管控区产出减半）
}
```

#### 3.2.6 资源 UI

- 顶部资源条：6 种资源图标 + 当前储备/上限
- 储备满时图标金光高亮，储备低时红色脉冲
- 点击资源图标 → 弹出详情：产出来源、消耗去向、历史曲线

### 3.3 工厂系统（生产任务分配 + 空闲提醒）⭐

**设计原则**：工厂建筑由"建筑模式"铺设（见 3.4），本节聚焦工厂建成后的生产任务分配与空闲提醒。保留 HOI4 的"经营质感"，但用强提醒降低操作负担。

#### 3.3.1 工厂类型

| 类型 | 作用 | 建造条件 |
|---|---|---|
| 民用工厂 | ① 建造其他建筑 ② 贸易换资源 ③ 生产消费品 | 任意己方省份 |
| 军用工厂 | 生产装备（步兵/装甲/炮兵/空军） | 任意己方省份 |
| 船坞 | 生产舰船（后续版本） | 沿海省份 |
| 基建 | 提升省份建造速度 / 补给容量 | 任意己方省份 |

#### 3.3.2 生产任务分配

**民用工厂任务池**：
- 建造队列（玩家指定要建的建筑）
- 贸易（用民厂换钢铁/石油等资源）
- 消费品（自动占用，剩余可分配）

**军用工厂生产队列**：
- 玩家选择装备类型（如"步兵装备 III"）→ 分配军厂数量 → 产出流入装备池
- 一条生产线可挂多军厂，效率随连续生产提升（HOI4 生产效率机制简化版）

#### 3.3.3 空闲提醒系统 ⭐核心

**触发条件**：任何工厂超过 5 秒未分配任务 → 触发提醒。

**提醒层级**（递进式，避免打扰但保证不漏）：

| 层级 | 触发 | 表现 |
|---|---|---|
| L1 静默高亮 | 空闲 5s | 省份/工厂图标呼吸光圈，顶部资源条数字闪烁 |
| L2 角标提示 | 空闲 10s | 顶部工厂图标显示红点 + 数字"3 座工厂空闲" |
| L3 浮窗建议 | 空闲 15s | 屏幕底部弹出浮窗："3 座民用工厂空闲，建议：① 建造军用工厂 ② 贸易换钢铁 ③ 忽略" |
| L4 自动暂停 | 空闲 30s（仅单机） | 自动暂停 + 全屏弹窗，强制玩家处理 |

**快捷操作**（降低手动负担）：
- "一键平衡"：自动把空闲民厂分配到建造队列优先级最高项
- "自动贸易"：勾选后，空闲民厂自动按当前最缺资源贸易
- "生产线模板"：预设装备生产模板，一键应用到空闲军厂

**提醒配置**：
```
工厂空闲状态枚举：IDLE / WORKING / CONSTRUCTION
每帧扫描所有工厂，统计 IDLE 数量
if IDLE_COUNT > 0:
    更新顶部红点数字
    if IDLE_DURATION > 阈值: 升级提醒层级
```

#### 3.3.4 数据结构

```typescript
interface Factory {
  id: number;
  provinceId: number;
  type: 'civilian' | 'military' | 'dockyard';
  level: number;              // 等级，影响效率
  state: 'idle' | 'working' | 'construction';
  taskId: string | null;      // 当前任务 ID
  idleSince: number;          // 空闲起始 tick（用于提醒计时）
  productionProgress: number; // 生产进度 0-1
}

interface ProductionTask {
  id: string;
  type: 'construction' | 'trade' | 'equipment';
  target: string;             // 建筑类型 / 资源类型 / 装备类型
  assignedFactories: number[];// 分配的工厂 ID
  priority: number;           // 优先级
  progress: number;
}
```

### 3.4 建筑模式（建造视图）⭐

**设计原则**：借鉴 HOI4 的建造界面。玩家点击"建筑模式"按钮 → 进入建造视图 → 在地图上选省份、选建筑、放置。建筑模式是工厂/基建/资源开采建筑/仓储的唯一铺设入口，与工厂生产任务分配（3.3）解耦。

#### 3.4.1 进入建筑模式

```
主界面右下角"建造"按钮 → 进入建筑模式
  ├─ 地图高亮可建造省份（己方核心区绿色、管控区黄色、不可建灰色）
  ├─ 底部建筑选择栏：民厂/军厂/船坞/基建/开采井/仓储
  ├─ 左侧建造队列：当前排队中的建造任务
  └─ 顶部显示可用民用工厂数（建造需消耗民厂产能）
```

- 进入建筑模式后游戏继续推演（不自动暂停），玩家可手动暂停
- 建筑模式可与"调兵拉线"等模式互斥切换

#### 3.4.2 可建造建筑清单

| 建筑 | 作用 | 建造条件 | 消耗 |
|---|---|---|---|
| 民用工厂 | 提供建造产能、贸易、消费品 | 任意己方省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 军用工厂 | 生产装备 | 任意己方省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 船坞 | 生产舰船 | 沿海省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 基础设施 | 提升省份建造速度、补给容量、资源产出 | 任意己方省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 资源开采井 | 开采钢铁/石油/钨/铝（需省份有对应节点） | 有资源节点的省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 仓储建筑 | 提升资源储备上限（仓库/储油罐） | 任意己方省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 补给枢纽 | 扩展补给网络覆盖 | 任意己方省份 | 钢铁 ×N + 民厂产能 ×时间 |
| 防御工事 | 提升省份防御、减速进攻方 | 任意己方省份 | 钢铁 ×N + 民厂产能 ×时间 |

#### 3.4.3 建造流程

```
1. 选建筑类型（底部栏）
2. 在地图上点选省份放置
   ├─ 校验：省份归属、地形限制、槽位是否空
   ├─ 校验：钢铁是否足够、民厂是否有空闲产能
3. 确认放置 → 进入建造队列
4. 分配民用工厂数量（越多越快）
5. 建造进度条推进 → 完成入库
```

- 省份建造槽位：基础 1 槽，基础设施等级每 +1 增加 1 槽（上限 4）
- 多建筑可排队，按优先级施工
- 民厂产能可随时调整分配，实时影响建造速度

#### 3.4.4 建造加速

- 民厂数量加速：N 个民厂同时施工，速度 ×N（线性）
- 资源不足减速：缺钢铁时建造速度按比例下降（见 3.2.4）

#### 3.4.5 建造队列管理

- 队列优先级拖拽排序
- 可暂停/取消单个建造任务（已消耗资源不返还）
- "一键铺设"模板：预设建筑组合（如"工业省模板 = 2 民厂 + 1 基建"），一键应用到选中省份

#### 3.4.6 数据结构

```typescript
type BuildingType =
  | 'civilian_factory' | 'military_factory' | 'dockyard'
  | 'infrastructure' | 'mine' | 'storage' | 'supply_hub' | 'fort';

interface Building {
  id: number;
  provinceId: number;
  type: BuildingType;
  level: number;
  state: 'planned' | 'constructing' | 'active';
  constructionProgress: number; // 0-1
  assignedCivilianFactories: number; // 分配施工的民厂数
}

interface ConstructionQueueItem {
  id: string;
  buildingType: BuildingType;
  provinceId: number;
  priority: number;
  steelCost: number;
  timeCost: number;            // 基础建造时间（秒）
  assignedFactories: number;   // 分配的民厂 ID 列表
  progress: number;
}
```

### 3.5 国家焦点树

- 每国家一棵焦点树（MVP 先做 3 个主要国家）
- 焦点为**三选一卡牌**，每 60s 刷新一张（替代 HOI4 的 70 天等待）
- 焦点效果：给工厂 / 给部队 / 给政治点 / 解锁科技 / 领土宣称
- 分支选择影响后续可选项（保留 HOI4 决策树感）

### 3.6 科研

- 线性科技线（非树状），三选一推进
- 类别：工业 / 电子 / 步兵 / 装甲 / 炮兵 / 空军 / 海军
- 每条线 5-8 个节点，解锁后给全局 buff 或新兵种卡

### 3.7 战斗与拉线

- 部署部队到省份 → 画前线 → 下达攻势箭头
- 实时自动推演：组织度/硬度/软硬攻击/战斗宽度（HOI4 核心公式简化）
- 兵种卡：步兵 / 装甲 / 炮兵 / 空中支援（4 种，删除编制编辑）
- 战斗宽度按省份地形固定（平原 60 / 山地 30 / 城市 40）
- 结算界面显示「管控区域」与「部队撤离」（不显示具体撤离人数），用语与 3.9 术语脱敏一致

### 3.8 补给系统

- 前线补给条：从首都 → 补给枢纽 → 前线省份
- 过远自动衰减（每超出 1 跳减 15%）
- 补给不足：部队组织度恢复 -50%，攻击力 -30%
- 可建补给枢纽（建筑模式铺设，见 3.4）

### 3.9 外交

三按钮极简：
- 结盟（消耗政治点，加入/创建阵营）
- 发起区域争端（需争端决心达标）
- 贸易（用民厂换资源，资源进入储备池，见 3.2）

> **术语脱敏**：全文统一将「宣战」→「发起区域争端」、「占领」→「管控」、「伤亡」→「撤离」、「战争支持度」→「争端决心」。申报与宣传主打「策略经营」定位，弱化战争对抗标签（详见第 9 章审核合规）。

### 3.10 时间控制

| 模式 | 控制 |
|---|---|
| 单机 | 自由暂停 / 1x / 2x / 5x |
| 联机 | Host 控制全局；任何人可请求暂停，host 同意后生效；快进仅 1x/2x，5x 禁用 |

### 3.11 双模式分层

为覆盖抖音碎片化场景与核心策略玩家两类群体，系统提供两种模式：

| 模式 | 时长 | 系统范围 | 定位 |
|---|---|---|---|
| 快速对局模式 | 10-15 分钟单局 | 仅资源-工厂-战斗核心系统，预设建筑与发展路线 | 新手入口，降低决策成本 |
| 经典存档模式 | 长期存档玩法（见第 5 章） | 完整系统 | 核心策略玩家，从快速模式导流 |

- **存档隔离**：两种模式存档互不影响，分别独立存储
- **新玩家引导**：新玩家默认进入快速对局模式；首局完成后引导尝试经典存档模式
- **模式切换**：主界面可切换模式，切换不影响对方模式存档

### 3.12 助理模式

降低操作门槛，新手默认开启，老手可关闭：

- **助理功能**：
  - 自动分配空闲工厂（按建造队列优先级）
  - 自动调度补给（按前线需求）
  - 自动布防前线（按威胁等级部署）
- **玩家职责**：仅做焦点选择、科研方向、总攻时机等核心决策
- **行为树复用**：与联机 AI 接管（6.4）复用同一行为树核心，但参数更保守（助理不主动扩张、优先稳健运营）
- **可撤销**：助理所有操作记录日志，玩家可一键撤销最近助理操作

### 3.13 阶梯式新手引导

按日拆解系统，避免一次性信息过载：

| 阶段 | 内容 | 解锁模块 |
|---|---|---|
| Day1 | 资源与工厂（资源采集、工厂铺设基础） | 资源系统、建筑模式、工厂分配 |
| Day2 | 生产与部署（装备生产、部队部署） | 军厂生产、拉线部署 |
| Day3 | 作战与外交（拉线攻势、外交按钮） | 战斗、外交 |

- **任务式引导**：每阶段以任务形式推进（如 Day1 任务"建造 1 座民厂 + 采集 100 钢铁"）
- **逐步解锁**：未到对应 Day 的模块锁定不可用，避免信息过载
- **完成条件**：完成 Day1 任务后解锁 Day2，依此类推

### 3.14 单次会话目标

适配碎片化打开习惯，每次进入游戏提示本次会话可完成的目标：

- **目标示例**：「今日可完成 1 个焦点 + 升级 2 座工厂」
- **动态生成**：目标基于玩家当前存档进度生成（焦点进度、工厂等级、资源储备等）
- **展示位置**：主界面顶部目标卡片
- **完成奖励**：目标全部完成后卡片变绿，发放额外政治点/资源奖励

### 3.15 每日任务体系

配合长期存档培养日活习惯：

- **任务数量**：每日 3 个小任务，建造/生产/作战各 1
- **刷新规则**：北京时间 0:00 刷新，未完成任务不累计到次日
- **完成奖励**：政治点 / 资源 / 临时 buff（如"今日工业产出 +10%"）
- **展示**：主界面显示每日任务进度条，完成可领取奖励

---

## 4. 世界观与架空国名

**原则**：全架空代号，所有名称/旗帜/装备型号均为虚构，**地理国界照搬真实世界地图**（玩家代入感优先，名称层架空以规避审核）。

### 4.1 主要国家

> **国家不绑定固有意识形态**：开局后玩家可在三条发展路线中三选一——**工业集权线 / 公社共治线 / 联邦共和线**。路线影响焦点树分支与 buffs，但无意识形态敏感词。下表"默认发展路线建议"仅为新手推荐，玩家可任意选择。

| 架空国名 | 历史原型（保留风格参考） | 默认发展路线建议 | 特色 |
|---|---|---|---|
| 铁十字联邦 | 德国（参考风格） | 工业集权线 | 装甲突击、闪击战 |
| 赤色同盟 | 苏联（参考风格） | 公社共治线 | 人海、工业纵深 |
| 自由合众国 | 美国（参考风格） | 联邦共和线 | 工业巨头、跨海支援 |
| 王冠群岛 | 英国（参考风格） | 联邦共和线 | 海军、全球殖民地 |
| 樱岛协约 | 日本（参考风格） | 工业集权线 | 海军、岛屿跳板 |
| 法兰克帝国 | 法国（参考风格） | 联邦共和线 | 防御工事、陆军传统 |

### 4.2 阵营

> **阵营名完全架空**，下表对应关系仅供开发参考，不出现在游戏内文案。

| 架空阵营 | 历史原型（仅供开发参考） |
|---|---|
| 钢铁轴心 | 轴心国 |
| 自由同盟 | 同盟国 |
| 第三国际 | 共产国际 |

### 4.3 领袖

用代号，不用真实姓名肖像（**代号仅供风格参考，不指向任何真实历史人物**）：
- 铁十字联邦 → "铁血首相"
- 赤色同盟 → "红军统帅"
- 自由合众国 → "新政领袖"
- 王冠群岛 → "雪茄首相"
- 樱岛协约 → "幕府大将"

### 4.4 装备命名

架空型号 + 简化图标（型号完全架空，仅参考二战装备风格，不直接对应真实型号）：
- 坦克：参考二战装甲风格 → "铁卫-I 型""铁卫-II 型"
- 战机：参考二战战机风格 → "苍鹰-I 型"
- 步枪：参考二战步枪风格 → "制式步枪 甲型"

### 4.5 地图（真实国界）

**原则**：照搬真实世界地图国界，提供强代入感；省份内部做抽象化划分，城市用代号。

- **国家级轮廓**：照搬真实世界国界（Natural Earth / GADM 矢量数据简化），玩家一眼认出"这是德国""这是苏联"
- **省份划分**：在真实国界内做抽象化省份切割（不照搬真实行政区划），按战略价值划分（核心区/管控区/殖民据点）
- **城市**：用图标标记，不使用真实城市名（"联邦首都""工业重镇"等代号）
- **资源节点**：钢铁/石油/钨/铝按真实地理大致分布（如鲁尔区钢铁、巴库油田），但用架空矿名
- **地图数据**：矢量轮廓简化到 <500KB（主包内），省份属性配置表分包加载
- **投影**：等积投影（保证面积比例真实，影响战略判断）

---

## 5. 存档系统（借鉴 HOI4）

### 5.1 存档模型

HOI4 的存档是**持续型**：一局游戏对应一个存档，可跨多次会话推进。本游戏复刻此模型。

```
单存档 = 一个国家的一局游戏（开局 → 玩家弃档才结束）
├─ 自动存档：每 30s + 关键事件（焦点完成/发起区域争端/管控城市）
├─ 手动存档：玩家随时存
└─ 多存档槽：3 个（单机）+ N 个（联机 host 存档）
```

### 5.2 单机存档

| 项 | 设计 |
|---|---|
| 存储 | `tt.setStorage`（单 key ≤1MB，总 ≤10MB） |
| 单存档体积 | <300KB（状态差分压缩 + 旧日志裁剪） |
| 存档槽 | 3 个，玩家可删可覆盖 |
| 加载耗时 | 1-2s（含解压 + 状态重建） |

**存档结构**：
```typescript
interface SaveSlot {
  version: string;
  countryId: string;
  scenarioId: string;
  createdAt: number;
  lastPlayedAt: number;
  // 静态数据
  unlockedFocuses: string[];
  researchProgress: ResearchState;
  // 动态状态（增量压缩）
  worldState: WorldSnapshot;      // 省份归属/部队/工厂/补给
  // 输入回放日志（最近 N 步，用于撤销/调试）
  inputLog: InputEntry[];         // 裁剪到最近 500 步
  // 元数据
  stats: GameStats;               // 决胜数/管控数/时长
}
```

**差分压缩**：世界状态只存与上一快照的差异（省份归属变化、部队移动），每 10 次差分存一次全量快照。

### 5.3 联机存档（借鉴 HOI4 主机制）⭐

HOI4 联机核心机制：
- **Host 持有存档**，其他玩家 join host 的游戏
- 游戏持续推演，可保存进度下次继续
- 玩家可随时 drop in/out，离开后 AI 接管其国家
- Host 退出则游戏结束（或迁移 host）

本游戏复刻：

| 机制 | 实现 |
|---|---|
| 存档归属 | **Host 本地存档** + 服务器快照备份（每 60s） |
| 存档标识 | roomId 关联一份联机存档，独立于单机存档槽 |
| 玩家加入 | 好友点击分享链接 → 带 roomId 加入 → host 推送当前状态快照 → 客户端追帧到最新 |
| 玩家离开 | 离开玩家国家由 AI 接管（借鉴 HOI4） |
| Host 迁移 | host 异常断线 → 服务器选最低延迟玩家为新 host → 推送最近快照 |
| 存档续玩 | host 下次开局可加载上次的联机存档，邀请原玩家继续 |
| 存档体积 | 联机存档同样 <300KB，host 本地存 + 服务器 CDN 快照 |

**联机存档与单机存档隔离**：联机对局的进度不写回单机主存档，但联机胜负可给单机主存档发奖励（如联机赢一场，单机+500 政治点）。

---

## 6. 联机系统（借鉴 HOI4）

### 6.1 架构：Host 权威 + 帧同步

```
玩家A (Host)  ←─WebSocket─→  信令服务器 (Node+ws)  ←─WebSocket─→  玩家B/C/D
   │                              │
   │                              └─ 职责：房间/对时/帧转发/快照备份/host迁移
   │
   └─ Host 额外职责：
       ├─ 帧提交权威（收集所有玩家输入 → 按帧号 commit → 广播）
       ├─ 存档持有（本地存 + 推送快照到服务器）
       └─ 暂停/快进控制
```

**为什么 Host 权威而非纯对等**：
- 借鉴 HOI4 主机制，玩家心智模型熟悉
- Host 作为单一帧提交源，避免对等架构的冲突仲裁难题
- 服务器仍只做中转，成本极低

### 6.2 帧同步参数

| 参数 | 值 | 理由 |
|---|---|---|
| 逻辑 tick | 10Hz (100ms) | 策略游戏操作密度低 |
| 提交缓冲 B | 150ms | 容忍移动网络抖动 |
| 输入缓冲 K | 2 帧 | 平衡延迟与同步 |
| 回滚窗口 | ≤4 帧 | 策略游戏可接受 |
| 带宽 | <2KB/s/人 | 10Hz × ~15 字节/帧 |

### 6.3 玩家 Drop In/Out（借鉴 HOI4）⭐

| 场景 | 处理 |
|---|---|
| 玩家加入 | host 推送当前状态快照 → 新玩家追帧到最新 → 接管其国家（若之前 AI 接管） |
| 玩家临时离开 | 其国家由 AI 接管，对局继续 |
| 玩家回来 | 重新 join 同 roomId → 接回原国家 |
| Host 离开 | 服务器选最低延迟玩家为新 host → 推送快照 → 继续 |
| 全员离开 | 存档保留 24h，host 可重新开局续玩 |

### 6.4 AI 接管

离开玩家的国家由 AI 接管，AI 行为：
- 工厂：自动平衡（按当前最缺资源/装备分配）
- 焦点：按预设优先级树选择
- 战斗：防御优先，被攻击时反击
- 难度：中等（不主动扩张，避免破坏平衡）

### 6.5 协议格式

```typescript
// 客户端 → Host（经服务器转发）
interface ClientInput {
  type: 'input';
  frameId: number;
  playerId: string;
  actions: PlayerAction[];  // 本帧操作：分配工厂/选焦点/拉线...
}

// Host → 所有客户端（经服务器广播）
interface CommittedFrame {
  type: 'frame';
  frameId: number;
  inputs: { [playerId: string]: PlayerAction[] };
  hash: string;             // 状态哈希（反作弊校验）
}

// 控制
interface ControlMsg {
  type: 'pause' | 'resume' | 'speed';
  by: string;               // 发起者
  speed?: 1 | 2;
}

// 存档同步
interface SaveSync {
  type: 'snapshot';
  frameId: number;
  data: CompressedSnapshot; // 压缩快照
}
```

### 6.6 确定性命门

- 全定点数 Q16.16，禁浮点参与判定
- 固定 tick 驱动，禁 delta 参与逻辑
- 随机数 host 下发 seed，每对象独立 PRNG
- 容器遍历固定 id 升序
- 逻辑/渲染分离，渲染只读逻辑影子拷贝

---

## 7. 技术架构

### 7.1 客户端目录结构

```
src/
├─ core/                  # 纯逻辑层（确定性，可单测，无 Cocos 依赖）
│   ├─ simulation/        # 固定 tick 推演
│   │   ├─ resource.ts        # 资源产出/储备/未用保留 ⭐
│   │   ├─ building.ts        # 建筑模式：建造队列/槽位/进度 ⭐
│   │   ├─ industry.ts        # 工厂生产任务/产出
│   │   ├─ production.ts      # 装备生产队列
│   │   ├─ supply.ts          # 补给流动
│   │   ├─ combat.ts          # 战斗结算
│   │   └─ research.ts        # 科研推进
│   ├─ determinism/       # 定点数、PRNG、有序容器
│   ├─ state/             # 状态机 + 快照 + 差分
│   └─ ai/                # AI 接管逻辑
├─ net/                   # 网络层
│   ├─ frame-sync/        # 帧同步：输入收集/预测/回滚/哈希
│   ├─ host-authority/    # Host 权威帧提交
│   ├─ signaling-client/  # WebSocket 信令
│   ├─ drop-in-out/       # 玩家加入/离开/AI 接管
│   └─ reconnect/         # 断线追帧
├─ game/                  # 游戏玩法（依赖 core）
│   ├─ focus-tree/        # 国家焦点三选一
│   ├─ resource-ui/       # 资源条 + 储备详情 ⭐
│   ├─ building-mode/     # 建筑模式：选建筑/放省份/队列 ⭐
│   ├─ factory-ui/        # 工厂生产任务分配 + 空闲提醒系统 ⭐
│   ├─ combat/            # 拉线攻势
│   └─ diplomacy/         # 三按钮外交
├─ render/                # Cocos 渲染层（只读 core 影子）
│   ├─ map/               # 2D 真实国界地图 + 省份抽象 + 拉线交互
│   ├─ ui/                # 卡牌/面板/焦点树
│   ├─ alerts/            # 空闲工厂提醒浮窗 + 资源不足警告 ⭐
│   └─ time-control/      # 暂停/1x/2x/5x
├─ platform/              # StarkSDK 适配
│   ├─ storage/           # tt.setStorage 存档封装
│   ├─ share/             # 分享带 roomId
│   └─ ads/               # 激励视频/插屏（仅单机）
└─ main.ts
```

**关键原则**：`core/` 是纯 TS 无 Cocos 依赖，可单测、可跑无头模拟——帧同步确定性的命根子，必须与渲染彻底解耦。

### 7.2 信令服务器

```
server/
├─ room.ts            # 房间管理（创建/加入/匹配/退出）
├─ frame-relay.ts     # 帧转发 + 帧号对齐
├─ host-migration.ts  # Host 迁移
├─ snapshot-cache.ts  # 快照备份（最近 30s + 每 60s 全量）
├─ hash-check.ts      # 状态哈希校验记录
└─ main.ts            # Node + ws 入口
```

服务器**不跑游戏逻辑**，只做中转与缓存。单进程 2C4G 可带几百房。

### 7.3 包体与性能

- 主包 <4MB：核心逻辑 + 启动 UI + 1 张默认地图
- 分包：每个剧本/国家包 1-3MB，按需下载
- 纹理图集 + 程序化生成地图（不存大位图）
- 目标 FPS ≥50，运行时内存 <150MB（中端安卓机红线）

---

## 8. 商业化

### 8.1 商业化广告位（按场景分类）

核心原则：**全面不卖数值、局内不卖任何东西（单机+联机一致无广告）、局外通过外观/内容广告变现**

**局内**（单机对局 + 联机对局）：无任何广告（工厂不提供加速按钮、焦点不提供刷新按钮、生产不提供双倍按钮；见 8.2）

**局外广告位**（主菜单/结算/商店/存档管理，不卖任何数值）：

| 场景 | 广告形式 | 触发 |
|---|---|---|
| 外观/皮肤解锁 | 激励视频 | 看广告 → 解锁国家主题色/地图皮肤/UI 主题/部队图标（纯装饰，不影响数值） |
| 内容解锁 | 激励视频 | 看广告 → 解锁新剧本/新国家/新焦点树分支（可用性解锁，非数值优势） |
| 结算页 | 插屏 | 每局 1 次（纯曝光） |
| 主界面 | Banner | 非核心区（纯曝光） |

> 注：每日补给箱、离线收益双倍已移除（违反「不卖数值」原则）；工厂加速建造、刷新焦点卡、双倍工业产出已移除（违反「不卖数值 + 局内尽量不卖」原则）；「解锁新剧本/国家」已并入局外内容解锁。局内（单机+联机）无任何广告，局外严格不卖任何资源/政治点/时间加速等局内数值。

### 8.2 联机模式

**无任何广告**。作为留存/传播引擎，靠好友拉新。

### 8.3 闭环

联机赢一场 → 单机主存档 +500 政治点（联机奖励，非广告）→ 引导回单机 → 局外商店看广告解锁外观/内容变现（局外不卖数值）。

---

## 9. 审核合规清单

- [x] 国名：全架空代号（铁十字联邦等）
- [x] 旗帜：架空设计，不照搬真实国旗
- [x] **地图国界：照搬真实世界国界**（玩家代入感，国界本身非审核红线）
- [x] 省份/城市：抽象化划分 + 代号城市名（不照搬真实行政区划）
- [x] 资源点：按真实地理大致分布但用架空矿名
- [x] 装备：架空型号名 + 简化图标
- [x] 领袖：代号，不用真实姓名肖像
- [x] 无血迹/尸体/残肢，死亡单位淡出
- [x] 健康游戏忠告 + 适龄提示 + 防沉迷
- [x] 无现金符号、无赌博、无低俗

> ⚠️ **风险提示**：照搬真实国界 + 战争题材存在审核不确定性。抖音规范明确禁止"使用真实地图素材、战争地点及战争名称"。本方案通过"国名架空 + 城市代号 + 省份抽象"降低风险，但真实国界本身可能被判定违规。建议提审前先做小范围预审，准备"完全抽象地图"备选包。

### 9.1 题材脱敏清单

> 配合真实国界地图方案，对题材做全面脱敏，降低整体审核风险。

| 脱敏维度 | 处理方式 |
|---|---|
| 意识形态标签 | 已删除（法西斯 / 共产主义 / 民主 → 工业集权线 / 公社共治线 / 联邦共和线） |
| 战争术语 | 已替换（宣战 → 发起区域争端、占领 → 管控、伤亡 → 撤离、战争支持度 → 争端决心） |
| 历史强关联元素 | 领袖代号仅供风格参考、装备型号架空（不直接对应真实型号）、阵营名完全架空不出现在游戏内文案 |
| 玩法定位 | 主打"策略经营"，战争作为可选冲突玩法（非强制） |

---

## 10. 开发里程碑

| 阶段 | 产出 | 验证点 |
|---|---|---|
| M1 单机核心 | core 推演 + 1 张真实国界地图 + **资源系统(产出/储备/未用保留)** + **建筑模式(铺设工厂/基建/开采井)** + **工厂生产任务+空闲提醒** + 焦点树 + 拉线攻势 + 暂停快进 + 本地存档 + 助理模式 + 快速对局模式 + 阶梯式新手引导 + 单次会话目标 | 能跑通开局→推进，资源能产出储备、建筑模式能铺工厂、工厂空闲有提醒，存档读写正常；助理模式能自动分配工厂、快速对局 10-15 分钟可完成、新手引导按日解锁 |
| M2 系统补全 | 科研/外交/补给/兵种卡/胜负判定 + 分包加载 + 每日任务体系 + 局外商店骨架（外观/内容解锁） + 关键事件召回推送 | 单局可玩到结局；每日任务 0:00 刷新、局外商店可看、推送能召回 |
| M3 联机 MVP | 信令服务器 + Host 权威帧同步 + 1v1 房间 + drop in/out + AI 接管 + 断线重连 | 双机能同步推演 10 分钟无脑裂，玩家离开 AI 接管正常 |
| M4 联机存档 | Host 存档 + 服务器快照 + host 迁移 + 存档续玩 | host 断线能迁移，存档能续玩 |
| M5 商业化 | 单机局内激励视频（加速/刷新/双倍产出）+ 局外商店（外观/皮肤解锁 + 内容解锁）+ 结算插屏/主界面 Banner | 单机局内可卖数值、联机无广告、局外只卖外观与内容 |
| M6 审核 | 提审 + 修复 | 过审上线（含真实国界预审 + 抽象包备选切换） |

---

## 11. 风险与对策

| 风险 | 等级 | 对策 |
|---|---|---|
| 帧同步脑裂（状态不一致） | 高 | 状态哈希每 16 帧校验，不一致回滚到最近快照 |
| Host 迁移失败 | 中 | 服务器存最近快照，迁移失败则回滚到快照重启 |
| 存档体积超限 | 中 | 差分压缩 + 输入日志裁剪，超限告警 |
| 工厂手动铺设操作负担 | 中 | 4 级空闲提醒 + 一键平衡/自动贸易/生产线模板 |
| 联机无广告拉低 ARPU | 中 | 联机奖励导回单机变现 |
| 长期存档与平台"短局"偏好冲突 | 中 | 每日焦点推送拉日活，单次会话 5-15 分钟 |
| 架空国名代入感不足 | 低 | 代号贴近历史原型，玩家能对应 |

---

## 12. 待细化项

- [ ] 资源节点的省份分布配置表（按真实地理大致分布）
- [ ] 资源储备上限与仓储建筑的数值曲线
- [ ] 建筑模式的省份选址策略（地形/资源加成）
- [ ] 建造队列优先级与民厂分配算法
- [ ] 空闲工厂提醒的 UI 视觉规范
- [ ] 焦点树具体节点设计（每国家）
- [ ] 战斗推演公式参数表
- [ ] 帧同步协议二进制编码（位级压缩）
- [ ] Host 迁移的具体选举算法
- [ ] AI 接管的行为树
- [ ] 真实国界地图的矢量数据来源与简化方案
- [ ] 真实国界审核风险评估与抽象备选包

---

**下一步**：建议从 M1 单机核心启动，优先搭 `core/` 确定性骨架，并按依赖顺序实现：资源系统（产出/储备/未用保留）→ 建筑模式（铺设工厂/基建/开采井）→ 工厂生产任务+空闲提醒。资源是建造和生产的硬约束，必须先于建筑/工厂完成。
