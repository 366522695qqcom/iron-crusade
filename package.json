{
  "name": "hoi4-mini",
  "version": "0.1.0",
  "description": "HOI4-inspired deterministic strategy mini-game for Douyin",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "typescript": "^5.4.0"
  }
}
